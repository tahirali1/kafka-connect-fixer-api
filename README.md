# kafka-connect-fixer-api
A simple kafka connect program. Though not a very useful example but the focus is to play around with connect framework

# Building

You can build the connector with Maven:

mvn clean package
